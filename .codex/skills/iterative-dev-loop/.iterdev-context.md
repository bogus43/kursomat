# .iterdev-context.md — runtime context for iterative-dev-loop
# Loaded by the skill before iteration 1. Highest priority — overrides .iterdev.yml.
# Remove sections you don't need.

## Scope
# - Module: src/            # focus only on this directory
# - Ignore: tests/        # never touch these directories
# - Ignore: vendor/

## Constraints
# Toolchain and compiler flags
# Compiler: arm-none-eabi-g++ -std=c++17 -Wall -Werror
# Build command: make -C build/ all
# No dynamic allocation
# No STL containers (bare-metal)

## Priority
# Describe what matters most in this session.
# Examples:
# Fix communication errors at I2C address 0x50, frame 0x51.
# Reduce stack usage in interrupt handlers.
# Eliminate all compiler warnings in hal/ module.

## Parameter overrides
# Override .iterdev.yml for this session only.
# MAX_FILES_CHANGED: 3
# MAX_LINES_CHANGED: 100
# ALLOWED_TYPES: [fix]
# ITERATIONS: 5

## Notes
# Any additional hints for the model.
# Examples:
# Register map is in docs/registers.md — refer to it for any HAL changes.
# Do not change public API in include/ — backward compatibility required.
